---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mqttui.${DOMAIN}
  namespace: home-automation
spec:
  secretName: mqttui.${DOMAIN}
  dnsNames:
    - ${HOST_MQTTUI}.${DOMAIN}
  issuerRef:
    kind: ClusterIssuer
    name: lets-encrypt

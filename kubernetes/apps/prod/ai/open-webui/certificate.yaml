---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: open-webui.${DOMAIN}
  namespace: ai
spec:
  secretName: open-webui.${DOMAIN}
  dnsNames:
    - ${HOST_OPEN_WEB_UI}.${DOMAIN}
  issuerRef:
    kind: ClusterIssuer
    name: lets-encrypt

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: unraid.${DOMAIN}
  namespace: nas
spec:
  secretName: unraid.${DOMAIN}
  dnsNames:
    - ${HOST_UNRAID}.${DOMAIN}
  issuerRef:
    kind: ClusterIssuer
    name: lets-encrypt

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: qbittorrent.${DOMAIN}
  namespace: media
spec:
  secretName: qbittorrent.${DOMAIN}
  dnsNames:
    - ${HOST_QBITTORRENT}.${DOMAIN}
  issuerRef:
    kind: ClusterIssuer
    name: lets-encrypt

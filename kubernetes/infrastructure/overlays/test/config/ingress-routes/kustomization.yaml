---
resources:
  - ../../secrets/host-config.enc.yaml
  - ../../../../base/config/ingress-routes/traefik-dashboard.yaml

vars:
  - name: HOST
    objref:
      kind: ConfigMap
      name: host-config
      apiVersion: v1
    fieldref:
      fieldpath: data.host

---
resources:
  - ../../../../base/config/ingress-routes/traefik-dashboard.yaml

configurations:
  - env-var.yaml
vars:
  - name: HOST
    objref:
      kind: ConfigMap
      name: host-config
      apiVersion: v1
    fieldref:
      fieldpath: data.host

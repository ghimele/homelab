---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-dashboard
  namespace: cert-manager
spec:
  dnsNames:
  - "*.test.ghimele.com"
  issuerRef:
    kind: ClusterIssuer
    name: test-selfsigned
  secretName: selfsigned-secret

---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dashboard
spec:
  dnsNames:
  - "*.test.ghimele.com"
  issuerRef:
    kind: ClusterIssuer
    name: selfsigned-ca
  secretName: selfsigned-secret

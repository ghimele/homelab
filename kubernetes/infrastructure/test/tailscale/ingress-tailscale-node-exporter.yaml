---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tailscale-node-exporter
  namespace: monitoring
spec:
  defaultBackend:
    service:
      name: vmetrics-prometheus-node-exporter
      port:
        number: 9100
  ingressClassName: tailscale

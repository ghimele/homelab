---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pocket-id.${DOMAIN}
  namespace: security
spec:
  secretName: pocket-id.${DOMAIN}
  dnsNames:
    - ${HOST_POCKETID}.${DOMAIN}
  issuerRef:
    kind: ClusterIssuer
    name: lets-encrypt
